export type baseTypographyTypes = {
    value: string;
    fontSize: number;
    color: string;
};

export type baseButtonTypes = {
    name: string,
    backgroundColor: string,
    color: string,
    borderRadius: number,
    fontSize: number,
    width: number,
    height: number
};